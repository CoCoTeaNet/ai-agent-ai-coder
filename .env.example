# AI 智能 Agent Demo - 环境变量配置

# 必填：AI 模型 API 密钥
# 请在系统环境变量中设置，或者在运行前创建 .env 文件并设置此变量
AI_API_KEY=your_ai_api_key_here

# 可选配置（这些配置也可以通过代码设置）
# AI_API_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v1
# AI_MODEL_NAME=doubao-seed-code-preview-251028
# AI_TEMPERATURE=0.7
# AI_MAX_TOKENS=2048
# AI_TOP_P=0.9

# 关于 .env 文件的说明：
# 1. 不要将包含真实 API 密钥的 .env 文件提交到任何公共仓库
# 2. 确保此文件在 gitignore 中被忽略
# 3. 使用 .env.example 作为模板文件分享给其他人
# 4. 在运行应用前设置环境变量

# 使用方法（Windows）：
# 命令提示符：set AI_API_KEY=your_actual_key
# PowerShell：$env:AI_API_KEY='your_actual_key'
# 或者在 IntelliJ IDEA 中通过 Run/Debug Configuration 设置

# 使用方法（Linux/macOS）：
# export AI_API_KEY=your_actual_key
# 或者在运行前设置：AI_API_KEY=your_actual_key java -jar app.jar

# 注意：
# 绝对不要将真实的 API 密钥硬编码在代码中
# 绝对不要将包含真实密钥的 .env 文件提交到 GitHub
